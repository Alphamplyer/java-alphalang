var shouldContinueLoading = true;
var countByLoad = 4;
var freeSpace = 10;
var takeInLoadCount = 0;
while (shouldContinueLoading) {
    freeSpace = freeSpace - countByLoad;
    shouldContinueLoading = freeSpace - countByLoad > 0;
    takeInLoadCount = takeInLoadCount + 1;
}
print "Take in load count :";
print takeInLoadCount; // Expect 2
print "";


for (var i = 0; i < 10; i = i + 1) {
  print i;
}

// Expect
// 0
// 1
// ...
// 9